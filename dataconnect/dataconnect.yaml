specVersion: "v1beta"
serviceId: "possof"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "possof-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
